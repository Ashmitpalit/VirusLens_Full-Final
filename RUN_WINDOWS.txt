═══════════════════════════════════════════════════════════════
🛡️  VirusLens - Windows: One Unified Command
═══════════════════════════════════════════════════════════════

METHOD 1: Double-click the file (EASIEST)
───────────────────────────────────────────
Just double-click: run_windows.bat

───────────────────────────────────────────────────────────────

METHOD 2: One Command in Command Prompt
─────────────────────────────────────────
Open Command Prompt in the project folder and run:

run_windows.bat

───────────────────────────────────────────────────────────────

METHOD 3: One Command in PowerShell
─────────────────────────────────────
Open PowerShell in the project folder and run:

cmd /c run_windows.bat

Or directly:

.\run_windows.bat

───────────────────────────────────────────────────────────────

METHOD 4: One-Line PowerShell (All-in-One)
───────────────────────────────────────────
Copy and paste this entire line into PowerShell:

cd VirusLens_Full-Final; if (!(Test-Path venv)) { python -m venv venv }; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt -q; python -m streamlit run main.py --server.port 8501

───────────────────────────────────────────────────────────────

METHOD 5: Single Batch Command (Command Prompt)
─────────────────────────────────────────────────
Copy and paste this into Command Prompt:

cd /d "C:\path\to\VirusLens_Full-Final" && if not exist venv python -m venv venv && call venv\Scripts\activate.bat && pip install -r requirements.txt -q && python -m streamlit run main.py --server.port 8501

(Replace C:\path\to\ with your actual path)

───────────────────────────────────────────────────────────────

RECOMMENDED: Just use run_windows.bat
──────────────────────────────────────
The easiest way is to double-click run_windows.bat file.
It will:
  ✅ Check Python
  ✅ Create virtual environment (if needed)
  ✅ Install all dependencies
  ✅ Create .env template (if needed)
  ✅ Start the server
  ✅ Open browser automatically

───────────────────────────────────────────────────────────────

